{
  "name": "tokokasir",
  "version": "1.0.0",
  "description": "Sistem POS (Kasir) + Pemesanan Online terintegrasi. Zero-dependency (Node built-in http + node:sqlite).",
  "type": "commonjs",
  "engines": { "node": ">=22.5.0" },
  "scripts": {
    "start": "node server.js"
  }
}
